# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
env/

# Environment / secrets
.env
.env.*
!.env.example

# Jupyter
.ipynb_checkpoints/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Prophet / cmdstan build artefacts
*.stan
*.hpp

# reports/ IS committed: it is the output of `trackflation compare-all` and
# `generate-report` over the committed data, and the README links to it. Both are
# regenerable from data/processed/ with no network access.

# Scraper HTTP cache and raw scrape output (regenerate with `trackflation scrape`).
# data/processed/ IS committed: it is the exact input behind every number in the
# README, so the results can be checked without re-scraping.
data/cache/
data/raw/

# OS
.DS_Store
Thumbs.db
